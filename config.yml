title: ozon-docker
description: Docker course
template:
  repo: ozon-docker-template
  name: ozon-docker
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
